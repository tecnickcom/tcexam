; Production-leaning PHP defaults for the TCExam runtime image (plan Stage 7).
expose_php = Off
display_errors = Off
log_errors = On

; Headroom for PDF generation, TMX parsing and bulk question/OMR import.
memory_limit = 256M
upload_max_filesize = 32M
post_max_size = 32M
max_execution_time = 120
