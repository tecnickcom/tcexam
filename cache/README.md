This is the writable runtime cache directory.

It holds generated artifacts such as the per-language translation caches (`cache/lang/`),
rendered LaTeX formula images, OMR scan uploads (`cache/OMR/`) and other temporary multimedia
resources. Its contents are regenerated as needed and must be writable by the web-server user.
